import React from 'react';
import {Container, TextArea} from './styles';
import {Text} from 'react-native';

export default () => {
  return (
    <Container>
      <TextArea>Perfil</TextArea>
    </Container>
  );
};
